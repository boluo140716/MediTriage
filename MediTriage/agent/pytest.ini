[pytest]
# 测试集中在 tests/。多数是「集成 / 冒烟测试」，会真实调用本地 vLLM(:8000)
# 与 Milvus(medical-milvus:19530)，需在容器 medix-fix 内、MediTriage/agent 目录下、
# 服务已起且已 `pip install -e .` 后运行：python -m pytest -ra
# 纯单元回归（tests/test_regression_*.py）不依赖服务。
testpaths = tests
python_files = test_*.py
addopts = -ra
